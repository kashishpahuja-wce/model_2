
  # Image Processing Website

  This is a code bundle for Image Processing Website. The original project is available at https://www.figma.com/design/O5rbby00vd61i9IjVxkipe/Image-Processing-Website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  